# 変更履歴

[日本語](CHANGELOG.ja.md) | [English](CHANGELOG.md)

## v0.6.0

- Gemini の文字起こしに対応し、デフォルトに変更。
- 翻訳モデルを `gpt-5-nano` に変更し、速報翻訳の待ち時間を短縮。
- AI Insights の言語設定を追加し、更新間隔を30秒に変更。
- 手動 Commit を廃止し、設定画面を整理。

## v0.5.1

- 速報・確定版の翻訳と AI Insights のモデルを `gpt-5.6-luna` から `gpt-6-luna` に更新。
- AI Insights の入力をItem単位のuserメッセージに分割。先頭から連続する確定版にキャッシュ境界を指定し、速報以降はキャッシュ書き込み対象外に。

## v0.5.0

- メインパネル左側に AI Insights を追加。同じ話題を統合した All Topics と、独立した Current Topic を表示し、タイトルを押すと概要を展開。
- 表示対象の書き起こし・翻訳を、翻訳処理とは独立して1分ごとに `gpt-5.6-luna` で要約。翻訳先の言語で表示。
- AI Insights に手動更新ボタンを追加。送信中はアイコンが回転し、重複送信を防止。
- 入力に変更がない場合や解析中は送信をスキップし、失敗時は最後の概要を保持。停止・30分の自動停止・画面破棄時に解析を中断。書き起こしのClearはInsightsの消去・中断に連動させない。

## v0.4.0

### 文字起こし・翻訳

- 低遅延な `gpt-live-transcribe` の flush 文字起こしと並行して、60秒ごとに `gpt-transcribe` の確定版を生成する機能を追加。
- 最新の確定版の終了時刻以前に完了した flush item を置換し、入力中の flush item は表示し続けるよう改善。

## v0.3.1

- `idle` 以外で Settings を開く際、停止確認を表示するように改善。

## v0.3.0

### 文字起こし・翻訳

- 文字起こしモデルを `gpt-live-transcribe` に変更。
- Prompt・Keywords・入力言語を設定できるように改善。

### 操作・表示

- 設定画面に言語選択を集約。
- メインパネルの文字サイズ切り替えとレイアウト改善。

## v0.2.1

### 文字起こし・翻訳

- `Commit` による文字起こしの手動確定と、長い発話中の翻訳更新を追加。

### 操作・表示

- セッションの状態に応じて `Start` と `Stop` を切り替えるよう改善。

## v0.2.0

### 文字起こし・翻訳

- 文字起こしモデルを `gpt-realtime-whisper`、翻訳モデルを `gpt-5.6-luna` に変更。
- 文字起こしと翻訳を逐次更新するよう改善。

### 操作・表示

- 時刻表示と自動スクロールを追加。
- セッションを30分で自動停止するよう改善。

## v0.1.0

- `gpt-4o-transcribe` を使った、ブラウザでのリアルタイム文字起こし・翻訳を初期実装。
